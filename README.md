# binary-search

Algorithm using a binary search.

The objetive is to evaluate different cases where given a dictionarie with a list of numbers in ascending order it returns the correct index position of the first and last number given as a target.

The target is a number on the list, and the output is the index position of that number on the list.
